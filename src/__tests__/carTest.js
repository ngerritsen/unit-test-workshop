import * as car from '../car';

describe('showSpeed()', () => {
  test('Shows the speed on the screen.', () => {
  });

  test('Shows the speed on the screen only once.', () => {
  });
});

describe('showFuelPercentage()', () => {
  test('Shows the fuel percentage on the screen.', () => {
  });
});

describe('Low fuel', () => {
  test('Shows a message when the fuel is getting low.', () => {
  });
});

describe('showPosition()', () => {
  test('Shows the gps location on the screen.', () => {
  });
});
